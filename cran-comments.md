## Submission notes

This is the initial CRAN submission for `DSIR`.

Maintainer: Shanlong WHO <dings@who.int>

`DSIR` is not a rename of the existing CRAN package `DSI` by
Yannick Marcon. It is an unrelated package for global health data
analysis, renamed to `DSIR` specifically to avoid a name clash
with that package.

## Test environments

* local Windows 11, R 4.5.3
* GitHub Actions: ubuntu-latest (release), windows-latest
  (release), macos-latest (release), ubuntu-latest (devel)

## R CMD check results

0 errors | 0 warnings | 1 note

The single NOTE is the standard "new submission" note.

## Reverse dependencies

None (new package).
