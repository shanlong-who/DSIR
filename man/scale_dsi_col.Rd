% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scale_dsi_col.R
\name{scale_dsi_col}
\alias{scale_dsi_col}
\alias{scale_y_dsi_col}
\alias{scale_x_dsi_col}
\title{Continuous Scales for DSIR Bar / Column Charts}
\usage{
scale_y_dsi_col(...)

scale_x_dsi_col(...)
}
\arguments{
\item{...}{Arguments forwarded to the underlying
\code{\link[ggplot2:scale_y_continuous]{ggplot2::scale_y_continuous()}} / \code{\link[ggplot2:scale_x_continuous]{ggplot2::scale_x_continuous()}}.}
}
\value{
A \code{ggplot2} Scale object, to be added to a plot with \code{+}.
}
\description{
Thin wrappers around \code{\link[ggplot2:scale_y_continuous]{ggplot2::scale_y_continuous()}} and
\code{\link[ggplot2:scale_x_continuous]{ggplot2::scale_x_continuous()}} that remove the default lower expansion
so that columns sit flush with the axis — the convention for WHO and
most publication-style bar charts. The upper expansion is preserved at
5\% so the tallest column has breathing room above (or to the right of) it.
}
\details{
Use \code{scale_y_dsi_col()} for vertical bars (\code{geom_col()} / \code{geom_bar()})
and \code{scale_x_dsi_col()} when bars are horizontal (via \code{coord_flip()} or
\code{geom_col(orientation = "y")}).

Pass any other \verb{scale_*_continuous()} argument
(\code{labels}, \code{breaks}, \code{limits}, ...) through \code{...}.
}
\examples{
library(ggplot2)

# Vertical bars
ggplot(mtcars, aes(factor(cyl))) +
  geom_bar(fill = "#0093D5") +
  scale_y_dsi_col() +
  theme_dsi()

}
