% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme_dsi.R
\name{theme_dsi_facet}
\alias{theme_dsi_facet}
\title{DSIR ggplot2 theme for faceted plots}
\usage{
theme_dsi_facet(
  base_size = 12,
  base_family = "",
  accent = "#0093D5",
  grid_color = "grey92",
  strip_fill = "grey95",
  strip_color = "grey20",
  grid = c("both", "x", "y", "none"),
  legend_position = "bottom"
)
}
\arguments{
\item{base_size}{Base font size in points. Default \code{12}.}

\item{base_family}{Base font family. Default \code{""} (system default).
Set to \code{"sans"}, \code{"Arial"}, \code{"Helvetica"}, or \code{"Calibri"} for a
specific look. Empty default keeps the package portable on CRAN's
Linux check machines, where Calibri is unavailable.}

\item{accent}{Accent colour, kept for parity with \code{\link[=theme_dsi]{theme_dsi()}} so
that the argument set is interchangeable. Not currently used in the
facet variant — the panel border replaces the accent-coloured axis
lines. Default \code{"#0093D5"} (WHO blue).}

\item{grid_color}{Colour of the major grid (both axes). Default
\code{"grey92"}.}

\item{strip_fill}{Background fill colour for facet strips. Default
\code{"grey95"} — a light neutral grey. Avoid blues, which clash with
the WHO-blue \code{accent}.}

\item{strip_color}{Text colour inside facet strips. Default \code{"grey20"}.}

\item{grid}{Which direction(s) to draw major grid lines. One of
\code{"both"} (default — both horizontal and vertical), \code{"y"} (only
horizontal), \code{"x"} (only vertical), or \code{"none"}. Matches the
identical argument on \code{\link[=theme_dsi]{theme_dsi()}} so the two themes can be
swapped without changing other settings.}

\item{legend_position}{Position of the legend. Default \code{"bottom"}.
Pass \code{"none"}, \code{"top"}, \code{"right"}, or a numeric vector \code{c(x, y)}.}
}
\value{
A \code{ggplot2} theme object.
}
\description{
A sibling of \code{\link[=theme_dsi]{theme_dsi()}} tuned for faceted plots. Where \code{theme_dsi()}
uses half-frame axis lines and only horizontal grid lines — a look that
suits a single panel — repeating those across every facet looks heavy
and the panels run together. \code{theme_dsi_facet()} replaces the axis
lines with a light panel border, draws grid lines on both axes, gives
the strip a soft background, and inserts whitespace between panels.
}
\details{
Use \code{theme_dsi()} for single-panel plots and \code{theme_dsi_facet()} for
plots with \code{facet_wrap()} or \code{facet_grid()}. Shared elements (text
styles, title block, legend, plot margins) match \code{theme_dsi()} exactly,
so the two themes feel like the same family.
}
\examples{
library(ggplot2)
ggplot(mtcars, aes(wt, mpg)) +
  geom_point(size = 2, color = "#0093D5") +
  facet_wrap(~ cyl, labeller = label_both) +
  theme_dsi_facet() +
  labs(title = "Fuel efficiency by cylinder count",
       x = "Weight (1000 lbs)", y = "Miles per gallon")

}
\seealso{
\code{\link[=theme_dsi]{theme_dsi()}} for the single-panel sibling.
}
