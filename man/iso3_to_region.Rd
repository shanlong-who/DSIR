% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/iso3_to_region.R
\name{iso3_to_region}
\alias{iso3_to_region}
\title{Look Up the WHO Region for ISO3 Codes}
\usage{
iso3_to_region(iso3, long = FALSE)
}
\arguments{
\item{iso3}{Character vector of ISO3 codes. Case-sensitive
(uppercase, as in \code{\link{who_countries}}).}

\item{long}{Logical. If \code{TRUE}, return long-form region names
(e.g. \code{"Western Pacific"}). If \code{FALSE} (default), return the
short codes used elsewhere in DSIR: \code{"AFR"}, \code{"AMR"}, \code{"SEAR"},
\code{"EUR"}, \code{"EMR"}, \code{"WPR"}.}
}
\value{
A character vector the same length as \code{iso3}.
}
\description{
Maps ISO 3166-1 alpha-3 country codes to WHO region codes using
the \code{\link{who_countries}} dataset shipped with DSIR. Stays in sync with
WHO governance changes reflected in DSIR — for example, Indonesia's
reassignment from SEAR to WPR following EB156 (May 2025).
}
\details{
Codes that do not correspond to a WHO Member State return \code{NA}.
This includes Associate Members (Puerto Rico, Tokelau) and other
non-Member areas that some indicator data sets cover.
}
\examples{
iso3_to_region(c("PHL", "FRA", "USA", "COK"))
# "WPR" "EUR" "AMR" "WPR"

iso3_to_region(c("IDN", "JPN"), long = TRUE)
# "Western Pacific" "Western Pacific"  (Indonesia in WPR since May 2025)

# Non-Member areas return NA
iso3_to_region(c("PRI", "TKL", "PHL"))
# NA NA "WPR"
}
\seealso{
\code{\link{who_countries}}, \code{\link{wpro_cty}}.
}
