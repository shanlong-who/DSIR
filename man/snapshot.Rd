% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/snapshot.R
\name{snapshot}
\alias{snapshot}
\title{Snapshot an Expensive Expression to a Local File}
\usage{
snapshot(expr, file, refresh = FALSE, max_age = Inf)
}
\arguments{
\item{expr}{An expression producing the object to snapshot. Evaluated
lazily — only when no usable snapshot exists (see Details).}

\item{file}{Path of the snapshot file (conventionally \code{.rds}). Read
with \code{\link[=readRDS]{readRDS()}} and written with \code{\link[=saveRDS]{saveRDS()}}.}

\item{refresh}{Logical. Re-evaluate \code{expr} even if a snapshot exists?
Default \code{FALSE}.}

\item{max_age}{Numeric. Maximum acceptable snapshot age in \strong{days};
an older snapshot triggers re-evaluation as if \code{refresh = TRUE}.
Default \code{Inf} (a snapshot never expires).}
}
\value{
The snapshotted object: either the value of \code{expr} or the
contents of \code{file}, per the logic above. A message states which of
the two was used.
}
\description{
Evaluates an expression — typically an API pull such as a
\code{\link[=gho_data]{gho_data()}} / \code{\link[=gho_clean]{gho_clean()}} pipeline — and saves the result to an
\code{.rds} file. On later calls, the saved snapshot is read back instead
of re-evaluating the expression, so analyses re-run reproducibly and
offline once the data have been fetched.
}
\details{
The decision logic is:
\enumerate{
\item If \code{file} exists, \code{refresh = FALSE}, and the snapshot is younger
than \code{max_age} days, the snapshot is read and returned. \code{expr} is
\strong{not} evaluated.
\item Otherwise \code{expr} is evaluated. A usable result (anything other
than \code{NULL}, a 0-row data frame, or an empty list) is written to
\code{file} (directories are created as needed) and returned.
\item An \emph{empty} result — the signature of an unreachable API, since
DSIR's network functions fail soft — is never written, so a
failed refresh cannot destroy a good snapshot. If an older
snapshot exists it is returned instead, with a warning; otherwise
the empty result is returned as-is.
}

\code{snapshot()} deliberately does \strong{not} invent file names, manage a
cache directory, or hash arguments: you choose the path, so a project
can use a fixed name (\code{data/indicators.rds}) or a dated one, and the
file is plain \code{saveRDS()} output readable without DSIR.

To force a re-fetch, pass \code{refresh = TRUE} (or delete the file).
}
\examples{
path <- tempfile(fileext = ".rds")

# First call evaluates the expression and writes the snapshot
x <- snapshot(data.frame(a = 1:3), path)

# Second call reads the snapshot; the expression is not evaluated
y <- snapshot(stop("not evaluated"), path)
identical(x, y)

# Force a refresh
z <- snapshot(data.frame(a = 1:5), path, refresh = TRUE)
nrow(z)

unlink(path)
\donttest{
# Typical use: pin an API pull for a reproducible report
ncd <- snapshot(
  gho_clean(gho_data("NCDMORT3070", "country", wpro_cty)),
  file.path(tempdir(), "ncdmort3070.rds")
)
}
}
\seealso{
\code{\link[=gho_data]{gho_data()}}, \code{\link[=sdg_data]{sdg_data()}}, \code{\link[=bind_indicators]{bind_indicators()}} — the
typical producers of snapshotted objects.
}
