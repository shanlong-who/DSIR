% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/life_table.R
\name{life_table}
\alias{life_table}
\title{Period Life Table from Age-Specific Mortality Rates}
\usage{
life_table(
  age,
  mx,
  sex = c("total", "male", "female"),
  ax = NULL,
  radix = 1e+05
)
}
\arguments{
\item{age}{Numeric vector of age-group lower bounds, strictly
increasing, e.g. \code{c(0, 1, seq(5, 85, by = 5))} for a standard
abridged table. The last group is open-ended.}

\item{mx}{Numeric vector of age-specific mortality rates (deaths per
person-year), the same length as \code{age}. Must be non-negative, with
no missing values.}

\item{sex}{Character. \code{"total"} (default), \code{"male"}, or \code{"female"}.
Only used for the default infant and child \code{ax} (see Details).}

\item{ax}{Optional numeric vector overriding the default person-years
assumptions, the same length as \code{age}. \code{NA} elements fall back to
the defaults.}

\item{radix}{Numeric. The starting cohort size \code{l0}. Default
\code{100000}; use \code{1} for survivorship proportions.}
}
\value{
A tibble with one row per age group and columns:
\describe{
\item{age}{Age-group lower bound (as supplied).}
\item{n}{Width of the age interval; \code{Inf} for the open interval.}
\item{mx}{Age-specific mortality rate (as supplied).}
\item{ax}{Average person-years lived in the interval by those dying
in it.}
\item{qx}{Probability of dying in the interval; 1 in the open
interval.}
\item{lx}{Survivors at exact age \code{x} out of \code{radix}.}
\item{dx}{Deaths in the interval.}
\item{Lx}{Person-years lived in the interval.}
\item{Tx}{Person-years lived above exact age \code{x}.}
\item{ex}{Remaining life expectancy at exact age \code{x}. \code{NA} where
\code{lx} has reached 0.}
}
}
\description{
Builds a standard period life table from age-specific mortality rates
(nMx). Works with both abridged tables (age groups \verb{0, 1, 5, 10, ..., 85+}) and complete single-year tables (\verb{0, 1, 2, ..., 100+}); the last
age group is always treated as open-ended.
}
\details{
The conversion from the mortality rate \code{mx} to the probability of
dying \code{qx} uses the standard relation
\deqn{{}_nq_x = \frac{n \, {}_nm_x}{1 + (n - {}_na_x) \, {}_nm_x},}
where \eqn{{}_na_x} is the average number of person-years lived in the
interval by those dying in it. Values of \code{qx} are capped at 1 (with a
warning, since capping signals implausibly high rates). In the open
interval, \code{qx = 1} and \code{Lx = lx / mx}.

\strong{The \code{ax} assumption.} By default:
\itemize{
\item age 0 (when the first group is age 0 with width 1): the Coale-Demeny
West formulas keyed on \code{m0} (Preston, Heuveline and Guillot 2001,
Table 3.3), by \code{sex}. For \code{sex = "total"}, the male and female
values are averaged.
\item ages 1-4 (when the second group is ages 1-4): the corresponding
Coale-Demeny West formula.
\item all other closed intervals: \code{n / 2} (the midpoint assumption).
\item open interval: \code{1 / mx} (the life expectancy implied by a constant
rate).
}

Pass your own \code{ax} vector to override all of this, e.g. to match a
published table exactly.

Life expectancy at any tabulated age is read off the \code{ex} column:
\code{ex[1]} is life expectancy at birth when the table starts at age 0.
The table may also start above age 0 (e.g. \code{age = c(60, 65, ..., 85)})
to compute remaining life expectancy conditional on survival to the
first age.
}
\examples{
# Abridged life table for a typical middle-income mortality schedule
age <- c(0, 1, seq(5, 85, by = 5))
mx  <- c(0.0200, 0.0010, 0.0004, 0.0003, 0.0005, 0.0007, 0.0009,
         0.0012, 0.0016, 0.0022, 0.0032, 0.0048, 0.0075, 0.0120,
         0.0190, 0.0310, 0.0520, 0.0860, 0.1500)
lt <- life_table(age, mx)
lt

# Life expectancy at birth and at age 60
lt$ex[1]
lt$ex[lt$age == 60]

# Sex-specific infant ax (affects e0 slightly)
life_table(age, mx, sex = "female")$ex[1]

# Survivorship proportions instead of a 100,000 radix
life_table(age, mx, radix = 1)$lx
}
\references{
Preston SH, Heuveline P, Guillot M (2001). \emph{Demography: Measuring and
Modeling Population Processes.} Blackwell, Oxford. Chapter 3.

Coale AJ, Demeny P, Vaughan B (1983). \emph{Regional Model Life Tables and
Stable Populations.} 2nd ed. Academic Press, New York.
}
\seealso{
\code{\link[=age_standardize]{age_standardize()}} for age-standardized rates; \code{\link[=aarr]{aarr()}} for
indicator progress tracking.
}
