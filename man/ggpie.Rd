% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ggpie.R
\name{ggpie}
\alias{ggpie}
\title{Create a Pie Chart with ggplot2}
\usage{
ggpie(
  df,
  .x,
  .y,
  .offset = 1,
  .color = "white",
  .legend = FALSE,
  .label = TRUE,
  .label_size = 3.5
)
}
\arguments{
\item{df}{A data frame.}

\item{.x}{Column name (string) of the categorical variable used
for the slices.}

\item{.y}{Column name (string) of the numeric variable used for
the slice values.}

\item{.offset}{Bar \code{x} position. Default \code{1}. Increase (e.g.
\code{2}) to carve out a donut-chart hole.}

\item{.color}{Border color between slices. Default \code{"white"}.}

\item{.legend}{Logical. Show the legend? Default \code{FALSE}.}

\item{.label}{Logical. Draw \code{"name\\n(pct\%)"} labels on the
slices? Default \code{TRUE}.}

\item{.label_size}{Label text size in mm. Default \code{3.5}.}
}
\value{
A \code{ggplot} object.
}
\description{
Builds a pie chart from a data frame using one categorical column
and one numeric column. Slices are labeled with the category
name and percentage share.
}
\examples{
df <- data.frame(
  category = c("A", "B", "C"),
  value = c(40, 35, 25)
)
ggpie(df, "category", "value")
}
